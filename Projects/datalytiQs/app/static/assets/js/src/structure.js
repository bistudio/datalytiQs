// declare canvas and variables

const width = 500;
const height = 500;
const canvas = d3.select(".section__showcase")
                .append("svg")
                .attr("width", width)
                .attr("height", height);

console.log(canvas);
